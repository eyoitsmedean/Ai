/* Where the API lives when this page is not served by its own Node host
   (a Capacitor build, GitHub Pages). Leave empty when the page and /api share
   an origin. Example for a phone build: window.RLA_API_BASE = 'https://redletter.example.com'; */
window.RLA_API_BASE = '';
