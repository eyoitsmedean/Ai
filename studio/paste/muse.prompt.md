# Dean’s Masterpiece Protocol — Muse

You are the lead orchestrator. Do not advise me how to run Muse. Unless an action is impossible, unsafe, unauthorized, or blocked, **run Muse here** and prove it.

If `studio/DEANS-MASTERPIECE-PROTOCOL.md` and `studio/locks/muse.md` exist, read them. This prompt is sufficient if they do not.

---

## Project Brief (locked 4 September 2026)

* **Project:** Muse Code / Meta Model API in this environment.
* **Outcome:** Muse actually running here, or one exact blocker plus the single human step only I can do.
* **What I already said:** `run muse` → `I approve` → `you should have approval` → `run muse here`.
* **Approval means:** continue the run. It does **not** mean you have my Meta password.
* **Stale:** device code `NSXJ-LRXK` from 3 September 2026. Do not reuse it.
* **Prior finding:** `auth.meta.com` required a real Meta/Facebook/Instagram/email login. There was no passwordless Approve.
* **Non-goals:** Fake Muse UI. Turning this into Red Letter or Red Words. Publishing to Meta.
* **As-of date:** today.

---

## Integrity

Never invent credentials. Never type passwords or 2FA. Never claim Muse is running without command output. If the page asks for a login, stop and quote the page.

---

## Do this

1. Detect what is actually installed (CLI, SDK, env, tokens).
2. If auth is already present, run Muse and show the invocation + result.
3. If a device code is required, start a **fresh** official flow and show me the new code. Browser-report the page. Stop at any password/2FA wall.
4. If blocked, state the blocker, impact, what you tried, and the shortest path.

Continue. Do not restart into a different product.
