# Alignment Ledger — Cycle 1

**State of the Project (15 lines)**

WATCH household build. Public-shaped product is `/ask`. Folio at `/` is paper. Corpus is KJV. Human gate still open. Trust harness seals speech. Crisis on `/ask` stops; folio chat still comforts after 988. `LAUNCH.md` is not law. Waitlist copy is now labeled household-only. Five summits this cycle: shared crisis module + eval, silence on `/ask`, `/gate`, `/hear`, welcome WATCH copy. Advent 29 Nov 2026 is a date, not a ship. Do not publish.

| Piece | Bearing | Class | Action taken 2026-09-14 |
| --- | --- | --- | --- |
| `lib/scripture.js` spokenLookup | C1 C10 | On course | Kept. Crisis moved to `lib/crisis.js`. |
| `lib/ask.js` + `/ask` | C6 C7 C10 | On course | Added stillness after the page is set. |
| Folio `/api/chat` after 988 | C7 | Bad drift | Held. Still Dean’s call. |
| `LAUNCH.md` / `MARKET_STRATEGY.md` | C8 | Unblessed | Held on disk. Not deleted. |
| Welcome waitlist | C8 | Bad drift | Relabeled household note. Endpoint kept. |
| Pages workflow | C8 | Bad drift | Held. Welcome now says Pages is not a launch. |
| Human gate as markdown only | C9 | Gap | Filled: `/gate` + `/hear`. |
| Crisis regex in three clients | C7 | Gap | Filled: `CRISIS_SOURCE` + drift test. |
| No eval without a browser | C5 C7 | Gap | Filled: `npm run eval-ask`. |
| Unused WEB-era files / cors | C3 | Unblessed | Left in place. |
