# State of the Project

1. The room is a parchment folio: Today, Seek, Sit, Advisor, Journal, Seven Days, blessings.
2. One encouragement brain (`lib/curated.js`) writes client rooms and Seek JSON.
3. Morning rotation is now the same function on server and static host (`RLA_dailyForDate`).
4. Empty send and no-room are two calm lines, not the day’s word and not tribulation.
5. Crisis letters must *lead* with John 14:27, not merely stay inside a safe set.
6. `help.html` is a no-script card with official numbers; the service worker caches it first.
7. `atlas.html` shows twelve first words and what each room refuses.
8. `npm run sit` and `npm run first-word` prove the conscience without assembling files.
9. Eval 113/113 both composers and 79 unit tests passed this cycle (2026-09-14).
10. Model path, Pages merge, and the phone walk remain Dean’s.
11. Forty stays off-stage until the week of 10 February 2027.
12. Live map stays unnamed; named eBible witness stays beside it; no swap.
13. Helplines re-checked 2026-09-14; numbers unchanged; IASP/APA say a list is not a handoff.
14. Knowledge base lives in `docs/lodestar/`.
15. This cycle did not deploy.
