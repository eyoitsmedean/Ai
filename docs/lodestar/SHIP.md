# Ship Package — LODESTAR Cycle 1

Pre-test revision. Verification section will be completed after `npm test`, `npm run qa`, and `npm run eval`.

## 1. Summary
Intent Charter v1. Integrity room (Matthew 5:37). Letter contract on server and eval. Blessing from a finished letter. Night latch. Title-page ask. Eval 54. Knowledge base in `docs/lodestar/`.

## 2. Charter check
C1 advanced (Integrity + seam disclosure). C2 advanced (first-ask, blessing, night). C3 advanced (notice sentences). C4 advanced (contract + 54). C5 unchanged (no device, no deploy). C6 advanced (chip + contract). Proposed: keep C6.

## 3. How to use
Open `docs/lodestar/CHARTER.md`, then `docs/lodestar/INDEX.md`. Run the folio (`npm start`) and ask: “My boss wants me to lie.” Open `/?night=1` for the latch. `?fresh=1` → To ask → write a sentence.

## 4. Verification actually performed
Scholar URLs opened 2026-09-14 (see dossiers). Integrity verses looked up in-corpus this session. **Suite not yet run on this revision** (next step).

## 5. Assumptions and limits
C6 named as a goal (reversible). Night = 21:00–06:00 local. No YouTube. No Granola. No model key.

## 6. Authorization Requests
- Letter to Cambridge before any UK paid tier.
- Provision production API host + `ANTHROPIC_API_KEY` for model-path eval.
- Do not submit stores / deploy / spend.

## 7. Recruitment Orders
See SHIP §7 after review. Video: BibleProject Mammon; SAMHSA 988 active-rescue webinar.

## 8–14
Index: `docs/lodestar/INDEX.md`. Director’s Cut: `docs/lodestar/DIRECTORS_CUT.md`. Handoff at end of the Navigator’s user-facing Ship after tests.
