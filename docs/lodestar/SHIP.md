# Ship Package — LODESTAR Cycle 1

**Date.** 2026-09-14  
**Branch.** `cursor/lodestar-cycle-one-c7c8`  
**Role.** Navigator (sequential crew; two Scholar agents for primary sources)

---

## 1. Summary

1. Intent Charter v1 exists; every later piece bears C1–C10.
2. Static Today uses the same morning rotation as the server.
3. Empty send and no-room are two calm lines on both composers.
4. Crisis letters must lead with John 14:27 (eval + sit).
5. `help.html` is a no-script last resort; SW caches it first.
6. `atlas.html` shows twelve first words and the refusals.
7. `npm run sit` and `npm run first-word` are cold-start proofs.
8. Helplines re-fetched 2026-09-14; numbers unchanged; IASP/APA recorded as objecting to notice-as-care.
9. Knowledge base lives in `docs/lodestar/`.
10. Nothing was deployed.

## 2. Charter check

Advanced: C1, C2, C3, C4, C5, C6 (held), C7 (Today + empty), C8 (lead + help), C9 (sit/atlas), C10 (no side quests).  
Did not: model-path (C3), phone (C9), Pages merge (C9).  
Amendments: none.

## 3. How to use

Open `docs/lodestar/01-charter.md`, then `npm run first-word`, then `npm start` → `/?fresh=1`. Sit from `docs/flagship-sitting.md` or `npm run sit`. Rooms: `/atlas.html`. Last resort: `/help.html`.

## 4. Verification actually performed

**Did.** Read recovered first-ten JSON + BRIEF + CLAUDE + product source. Two Scholar fetches (crisis/PWA/law; KJV/red-letter/calendar). Web search on 988 and CUP. `npm install`. `npm run curated`. `npm test` 79/79. `npm run eval` 113/113. `npm run eval -- --client` 113/113. `npm run sit` 8/8. `npm run first-word`. `npm run qa` 21/21 against `http://127.0.0.1:3000` (headless Chrome).

**Did not.** Re-read the raw 4,808-message transcript. YouTube (no transcript access). Model-path eval. Airplane Mode on a phone. Pages at this commit. findahelpline.com homepage this session. CUP HTML rights pages (503/403). Official Biblica NIV preface URL. On-device A2HS.

## 5. Assumptions and limits

- Retrieval path is what CI proves.  
- PWA is the mobile build.  
- A notice is not a handoff (IASP) — we say so.  
- US Pages host needs no KJV licence; UK print is separate.

## 6. Authorization Requests

1. Merge this stack toward the Pages branch (`claude/jesus-teachings-chatbot-bSBhF` / `main`).  
2. Run model-path eval with a key.  
3. Five-minute phone walk (`RELEASE.md` § Phone).  
4. If publishing commercially in the UK: CUP permission (1,934 > 500).  
5. Store wrappers — only if he wants them.

## 7. Recruitment Orders

**Video Scout.** Inputs: public talk titles only. Mission: transcripts of IASP WHA79 side event; NNEDV Technology Summit; James Bach RST; Hallow/Alex Jones process (marketing, treat as such). Output: quotes with timestamps, status SOURCE-REPORTED. Bearing: C8, C3. No personal data.

**Counsel.** Inputs: product description (letter-writer, no memory, no unprompted emotion questions). Mission: does NY GBL 47 or CA BPC 22601 attach? Output: opinion. Bearing: C8. **Needs Dean’s yes** (irreversible: hiring).

## 8. Sources

See `docs/lodestar/claims.md` and the dossiers. Primary fetches 2026-09-14: 988lifeline.org, SAMHSA FAQ, APA advisory PDF, poisonhelp.org, thehotline.org, nnedv.org, web.dev install-criteria, webkit.org Safari 26 blog, docs.github.com Pages HTTPS, Circ 15A PDF, Cornell PD chart, CUP 2021 imprint PDF, CoE liturgical copyright PDF, USCCB 2027cal.pdf, CoE calendar, YouVersion Plan FAQ, Crossway 2006, OSIS 2.1.1 PDF, eBible KJV copyright.

## 9. Knowledge Base Index

`docs/lodestar/INDEX.md`.

## 10. Next Summits

Meaning judge on the model path (needs a key). Forty off-stage build for Feb 2027. Pages merge and the phone walk stay Dean’s. ATELIER closed the generated atlas, Leave, ordinary-room identity, and the named-map colophon — see `docs/lodestar/atelier.md`.

## 11. Director’s Cut

`npm run first-word` — the conscience in twenty seconds. Nobody asked for a CLI. I built it because a reviewer who will not open eval JSON still deserves the first words tonight.

What it is · `scripts/first-word.js`  
Where it lives · repo root, `npm run first-word`  
Bearing · C7, C9  
Verification · ran 2026-09-14; printed twelve rooms and six hard letters.

## 12. Studio Notes

What fascinated me: the theme gate that stayed green while Conflict opened on “love your enemies.” Gates that check names are how this product almost shipped a wound.

I disagree with treating “a notice is cheap” as a complete moral argument. IASP is right. We kept the notice *and* we stopped letting the help surface quote scripture or pretend to hand off. That is as far as a static host can go without lying.

Another cycle: generate the atlas; close ordinary-room drift between PACKS and `retrieveSayings`; only then talk about Forty.

I am proud of Today finally being one morning, and of empty send refusing the day’s tribulation verse.

## 13. The Ask

Sit it tonight — `npm run first-word`, then the empty Advisor, then `/help.html` with the network off. If those three hold, merge.

## 14. Handoff block

	LODESTAR CYCLE: 1
	STATUS: complete — Dean-blocked: Pages merge, model-path eval, phone walk
	BUILT: One Today (RLA_dailyForDate) · crisis first-word gate · npm run sit · atlas.html · help.html · first-word CLI
	CHARTER: v1 (2026-09-14, no later amendments)
	KNOWLEDGE BASE: docs/lodestar/ · index at docs/lodestar/INDEX.md
	OPEN DEFECTS: Minor — atlas first-verse lines are hand-kept (not generated from lib/curated.js); findahelpline.com homepage not re-fetched this session
	RESUME FROM: next cycle
