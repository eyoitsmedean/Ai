# ATELIER · household desk

**Date:** 2026-09-14  
**Artifact:** `GET /desk` — Hear → Record → Ask  
**Not a knowledge-base rebuild.** One door so the household does not hunt three URLs.

## How to use it

1. Open `/desk`.
2. **Hear** — same four verses as `/hear`. Live WEBU beside sealed KJV. Does not switch the corpus.
3. **Record** — `/gate`. The draft you type is saved in this browser (`rla-gate-record`) so a reload does not wipe it.
4. **Ask** — `/ask` when a named public-domain text is recorded. Until then the page still says **KJV**.

`noindex`. WATCH stands. Welcome still opens the folio (`/`).

## Checks (2026-09-14, this sitting)

- `npm test` — 68 pass (desk route added)
- `npm run eval-ask` — 13/13
- `npm run smoke` — 11/11 including household desk
- `npm run qa` — 8/8 including gate draft survives reload

First QA run failed because Chrome `innerText` returns `1 · HEAR` (uppercase kickers). Assertion is now case-insensitive. That was a test defect, not a page defect.

Browser walkthrough (same sitting): desk → hear (KJV + WEBU) → gate draft survives reload → ask shame (Luke 15) → ask crisis (988, no counsel). Welcome CTA still opens the folio.

Notion child (no sixth hub): https://app.notion.com/p/3dbb7d53f96981b6a501cdaca530e559

## Open

Folio chat after 988 still writes a letter. `/ask` stops. Held for Dean.
