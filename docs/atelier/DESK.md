# ATELIER · household desk

**Date:** 2026-09-14  
**Artifact:** `GET /desk` — Hear → Record → Ask  
**Not a knowledge-base rebuild.** One door so the household does not hunt three URLs.

## How to use it

1. Open `/desk`.
2. **Hear** — same four verses as `/hear`. Live WEBU beside sealed KJV. Does not switch the corpus.
3. **Record** — `/gate`. The draft you type is saved in this browser (`rla-gate-record`) so a reload does not wipe it.
4. **Ask** — `/ask` when a named public-domain text is recorded. Until then the page still says **KJV**.

`noindex`. WATCH stands. Welcome still opens the folio (`/`).

## Checks

Run after this note: `npm test`, `npm run eval-ask`, `npm run smoke`, `npm run qa`.

## Open

Folio chat after 988 still writes a letter. `/ask` stops. Held for Dean.
