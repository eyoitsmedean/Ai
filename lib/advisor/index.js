/**
 * Advisor map — LODESTAR Cycle 1.
 * Bearing: C2, C4
 *
 * This directory exists so the inventory path in the recovery brief is real.
 * Implementation lives in data/scripture.js (gate, verify, theme, thread).
 * server.js remains the HTTP + model shell. Do not treat this file as a second
 * copy of the safety gate.
 */
module.exports = require('../../data/scripture');
