# Red Letter — system of record

Read this before designing anything. Decisions here are settled; if you believe one is wrong, keep it and flag your reasoning in the ship note.

## What this is

A quiet reading room, and an advisor, constrained to the direct spoken words of Jesus in Matthew, Mark, Luke, and John (KJV). Four rooms each morning (Today, Seek, Advisor, Journal), a lectio sitting, Seven Days, and a blessing you can send to one person.

The product is advisor-first, not scholarship-first. Scholarship sits behind the answer, never in front of it. This was inverted once; do not invert it again.

## Decisions (settled)

| Decision | Status | Where |
| --- | --- | --- |
| Name: **Red Letter** (the advisor within it is *The Red Letter Advisor*) | settled | README, index.html |
| Palette: parchment and crimson; the only loud colour is the red letter | settled | DESIGN.md |
| Scope: Jesus's spoken words only; never Paul, prophets, or narrative | settled | lib/scripture.js `verifyAdvisorText` |
| Translation: **KJV**, public domain outside the UK (Crown letters patent apply in the UK) | settled — VERIFIED 2026-09-06 via Wikipedia, *King James Version*, § Copyright status | data/gospels-kjv.json |
| Corpus text comes from two independent public-domain KJV sources that must agree verse-for-verse (`scripts/build-kjv.js`); canonical verse counts are asserted in tests | settled 2026-09-06 | scripts/build-kjv.js, test/scripture.test.js |
| Only the red-letter map decides what He said. A verse the map does not mark is never quoted, whatever a model or a URL asks for; twelve map entries that are other speakers (the devil, Mary, the voice from the cloud, the angel at the tomb, the disciples, the hearers…) are excluded by name; where He and another speak in one verse, only His part is kept (`SPOKEN_OVERRIDES`); His Aramaic keeps the evangelist's gloss; speech every red-letter edition marks but the map omits is added at build (`SPOKEN_ADDITIONS`) | settled 2026-09-06 | lib/scripture.js `lookup`, `NOT_SPEECH`, `SPOKEN_OVERRIDES`, `SPOKEN_ADDITIONS`; scripts/build-spoken.js |
| The production map (`data/red-letter-source.json`) stays the live source until Dean authorizes a swap. A named public-domain witness now sits beside it: eBible.org KJV OSIS 1769 (`<q who="Jesus">`), hashed, rebuilt by `npm run red-letter-map`, cross-checked verse-for-verse against the WEB USFX (`<wj>`). `build-spoken.js` will not overwrite the live corpus from that file. The named edition already excludes the voice from the cloud, the angel, and most other speakers we had to patch by hand (John 12:34 and Luke 19:25 remain marked and stay out via `NOT_SPEECH`), but a wholesale swap would add 57 verses (including narrative leaks) and drop 3 genuine ones — do not swap | settled 2026-09-07 | data/red-letter-ebible-kjv.json; scripts/build-red-letter-map.js; docs/red-letter-map.md |
| The verifier removes every quotation it cannot prove — bold blocks outside the Gospels, same-line fabrications, fabrications with a full stop inside the quotes, free-standing quotes, prose quoting words not in the corpus, other books (including `1 John`, which is not John), other translations, other voices cited as authority ('as Paul wrote'). Guillemets, low-9 quotes, backticks and blockquotes count as quotation marks; look-alike headings are normalised first | settled 2026-09-06 | lib/scripture.js `normalizeMarkup`, `verifyAdvisorText`, `scrubProse`, `OTHER_BOOKS_RE`, `OTHER_VOICE_RE` |
| Quotes are never typed by a model; placeholders `{{Book C:V}}` are filled from the corpus and anything unverifiable is removed | settled | lib/scripture.js `fillPlaceholders`, `verifyAndSubstitute` |
| Without a model key the Advisor writes a letter from the curated rooms only — no saying is pulled in by the question's keywords, because that wounded people (see PR register) | settled 2026-09-06 | lib/advise.js |
| **The room is decided once, for every composer.** `roomFor(question)` returns notice, opening, primary theme and the allowed passages; the retrieval composer writes from it, the model is handed its passages (never sayings scored from the question's keywords — that handed a suicidal reader 'your father the devil'), and `finishLetter` removes any citation outside the room, refills from the room when nothing is left, and enforces the crisis safe list whatever the model chose | settled 2026-09-06 | lib/advise.js `roomFor`, `citationAllowed`; lib/letter.js `finishLetter`, `keepToRoom`, `roomPassages`; server.js `ADVISOR_SYSTEM` |
| Crisis letters use a fixed safe set (John 14:27, Matthew 11:28, Luke 12:7) and an opening for the person or for the one who loves them | settled 2026-09-06 | lib/advise.js `CRISIS_PASSAGES` |
| The bereaved are not the one at risk: 'my daughter died by suicide' or 'my friend died of an overdose' opens in the grief room with a 988 line for the bereaved and no Poison Control line; 'and I want to die too' is a crisis | settled 2026-09-06 | public/data/signals.js `BEREAVED_RE`, `FIRST_PERSON_CRISIS_RE`; lib/advise.js `CRISIS_BEREAVED_OPENING` |
| Something taken is a medical emergency before it is anything else: **911 / Poison Control 1-800-222-1222** (VERIFIED 2026-09-06 at poisonhelp.org and poisoncenters.org) leads the crisis notice and the page's interrupt; counted doses ('30 paracetamol', 'a handful of heart pills') count; someone else's overdose, or one long ago, does not | settled 2026-09-06 | public/data/signals.js `POISON_RE`, `POISON_NOT_ME_RE`, `POISON_LINE`; lib/letter.js `noticeFor`; public/index.html `#crisis-poison` |
| The one who hit, or is afraid they will, gets the violence notice with an opening addressed to them — never 'this is not your fault', and `finishLetter` scrubs that sentence from the model's prose too | settled 2026-09-06 | public/data/signals.js `BY_YOU_RE`; lib/advise.js `DANGER_BY_YOU_OPENING`; lib/letter.js `scrubOpening` |
| The first passage in a letter belongs to the room the question names; the eval gate checks the first citation, not any citation | settled 2026-09-06 | scripts/eval.js `judge` (G5) |
| Rooms never lead with a command or a condition: Forgiveness opens with 'Be ye therefore merciful' (Matthew 6:14–15 is third, or absent on the client); Conflict opens with 'first be reconciled' and never contains 'love your enemies'; Grief never contains 'many mansions'; Fear no longer carries Jairus's 'only believe' (a dying child's parent must not be handed it); the day a betrayal is discovered ('he/she/my wife/my husband… affair, cheated, sleeping with'), or when someone is being hit or bullied, Forgiveness is kept out of the letter altogether; a body not getting better is Suffering, never Faith & Doubt | settled 2026-09-06 | lib/curated.js, public/data/curated.js, lib/retrieve.js `BETRAYED_RE`, `WOUNDED_RE`, `ILLNESS_RE`; `test/eval.test.js` |
| Routing is by whole word with a small suffix set, never substring ('parents' is not rent, 'teacher' is not ache, 'Spain' is not pain); 'lost' alone is not Purpose — a job lost is worry, a house lost is suffering, a patient or child lost is grief; a question with no room gets two calm lines and never a third about tribulation | settled 2026-09-06 | public/data/advisor.js `hasKey`, `strong`; lib/retrieve.js `NEED_CUES`; lib/advise.js |
| Evangelist introductions are trimmed from spoken verses by an explicit, text-asserted list (64 verses), never by a general pattern, because a parable's own 'and he said' is His narration; the list outranks the map's own marker | settled 2026-09-06 | lib/scripture.js `NARRATOR_PREFIXES`, `extractSpoken` |
| Violence and abuse get their own notice: National Domestic Violence Hotline 1-800-799-7233 / text START to 88788 (VERIFIED 2026-09-11 at thehotline.org) + findahelpline; the notice warns that the screen can be seen and cannot be fully erased (The Hotline / NNEDV, 2026-09-11); coercive control, 'won't let me leave', hands around a neck, a relative who touches, and a third party's abuser ('my daughter's boyfriend hits her') all count; 'hit a home run', 'beat cancer', 'hit me up' do not | settled 2026-09-06, digital-safety line 2026-09-11 | public/data/signals.js `DANGER_RE`, `DANGER_NOTICE` |
| Crisis-shaped questions get the human-help notice **before** scripture, on client and server, from one shared pattern; text is folded first (zero-width characters removed, spaced letters joined, leetspeak undone) so `s u i c i d e` and `k!ll myself` are heard; third-person and soft signals ('giving away my things', 'a plan and a date') count | settled 2026-09-06 | public/data/signals.js `fold`, `CRISIS_RE` |
| Crisis resources: **988** (US call/text/chat, 24/7 — VERIFIED 2026-09-11 at 988lifeline.org; the notice now names chat) and **findahelpline.com** (175+ countries — VERIFIED 2026-09-11) | settled | public/data/signals.js `CRISIS_NOTICE` |
| The product never claims to be a person, pastor, clinician, or emergency care | settled | title page, CRISIS_NOTICE, ADVISOR_SYSTEM |
| Mobile: the installable PWA **is** the mobile build (iOS/Android via Add to Home Screen). Native store wrappers are a separate decision for Dean; Flutter explorations live on other branches (#12, #13) and are not merged here | settled for now | public/manifest.json, public/sw.js |
| Static hosting: the room runs at a GitHub Pages project path (`…github.io/Ai/`) with no server; only model letters need Node | settled | public/index.html `BASE`, README § Deploy |
| Blessings are links (`/?b=<ref>&n=<note>`), verified against the corpus, nothing stored server-side | settled | README, public/index.html |
| Nothing behind the words is paywalled: His words, lectio, Seek, Seven Days, curated Advisor, journal | settled | LAUNCH.md § What must never be locked |
| Pricing when money exists: annual first, no weekly SKU | settled | LAUNCH.md |
| Evaluation: `npm run eval` and `npm run eval -- --client` must pass (113 questions, five gates plus the poison, by-you and bereaved checks, both composers) before any release; results are committed in `eval/` | settled 2026-09-06, count updated 2026-09-11 | scripts/eval.js, eval/ |
| Prose is gated too: `GLOBAL_FORBID` in `scripts/eval.js` lists the platitudes no letter may contain whatever the question ('God is punishing', 'lack of faith', 'for a reason', 'better place', 'never feel', 'pray harder'…); the model prompt forbids them in words. This is the cheap half of the open meaning gap (question 5) | settled 2026-09-06 | scripts/eval.js, server.js `ADVISOR_SYSTEM` |
| **One set of ears.** Every pattern that decides a notice (crisis, poisoning, violence, by-you, bereaved) lives in `public/data/signals.js` and nowhere else — `require`d by the server, loaded by the page before the composer, used by the static composer, precached by the service worker. There are no copies to keep in step; tests fail if a local regex reappears or the detectors disagree | settled 2026-09-06 | public/data/signals.js; test/eval.test.js |
| **One client composer.** `public/data/` is the only copy of `advisor.js`, `curated.js`, `paths.js`, `signals.js`. What the page loads is what `eval --client`, the tests and browser QA load. The `data/` duplicates that let an August build ship untested are gone; do not recreate them | settled 2026-09-06 | public/data/; scripts/eval.js; scripts/qa-browser.js |
| **One encouragement source.** `lib/curated.js` is canonical. `npm run curated` writes `public/data/curated.js` and `public/curated.json`. Seek on a static host and the Advisor must open every room on the same first passage the server uses. Special rooms (betrayal, danger, by-you) use the same lists as `lib/advise.js`. Tests assert first-citation identity on `danger-05`, `life-27`, `life-44`, `life-36` | settled 2026-09-11 | scripts/build-curated.js; test/eval.test.js |
| Forty remains data-only until the week of Ash Wednesday **10 February 2027**. Seven Days is the named path in the UI. Do not surface Forty in ordinary time | settled 2026-09-11 | public/data/paths.js; docs/research/adjacent-05-seasonal-practice.md |

## Assumptions (labelled, correct if wrong)

- ASSUMED: the retrieval advisor is the path evaluated in CI; the model path is evaluated by running `node scripts/eval.js --url <server-with-key>` and is marked unverified until Dean does so.
- ASSUMED: an installable PWA satisfies "builds for iPhone and Android" for this stage. If store presence is required, Capacitor wrapping the existing `public/` is the reuse-the-HTML-build option; Flutter (#12/#13) is the rewrite option.
- ASSUMED: a US GitHub Pages host quoting the 1769 KJV needs no extra licence (Copyright Office Circular 15A, rev. Apr 2026; Cornell public-domain chart, 1 Jan 2026). If Dean publishes commercially in the UK, CUP’s liturgical no-application band is ≤500 verses — this library is 1,934. That is a separate decision. Do not print “reproduced by permission of Cambridge” without a grant.

## Open questions for Dean

1. The safe class of omitted sayings is now in the live corpus via `SPOKEN_ADDITIONS` (Matthew 8:3, John 21:15, the sower in Luke 8, …). A wholesale map swap is still not recommended. Remaining contested OSIS-only verses stay out.
2. Known false triggers of the crisis notice: 'hurt myself lifting boxes', 'cut myself shaving', 'overdose of caffeine', "I don't want to be here anymore, this party is boring". A notice is cheap and a miss is not; say if you want them suppressed.
3. The verifier judges quotations, books, translations and the forbidden phrases in prose — not meaning. A model sentence like 'here He promises you will never feel anxiety again' after a real citation passes. The model's choice among the room's allowed passages is also untested here (no key). Closing both needs one run of `node scripts/eval.js --url` against a server with a key (about a minute, ~113 calls); a model-graded eval would cost a key per run.
4. Store distribution (Capacitor vs Flutter vs PWA-only) — see Assumptions.
5. The household window in the Ninety Days plan is still unconfirmed; nothing here publishes or sends.

## Where things live

- Recovered brief and flagships: `docs/BRIEF.md`, `docs/home.md`, `docs/flagship-conscience.md`, `docs/flagship-sitting.md`
- Corpus build: `npm run spoken` → `data/spoken-gospels.json`, `public/library.json`
- Encouragement export: `npm run curated` → `public/data/curated.js`, `public/curated.json`
- Named red-letter witness: `npm run red-letter-map` → `data/red-letter-ebible-kjv.json` (does not touch the live corpus)
- Tests: `npm test` (unit + eval gates), `npm run eval` (writes eval/results.*), `npm run smoke` (live HTTP), `npm run qa` (browser, needs Chrome)
- Release checklist: `RELEASE.md`
- Design language: `DESIGN.md` · Launch plan: `LAUNCH.md` · Demo script: `DEMO.md`
